export interface UserInfo {
    id:string;
    name: string;
    phone: string;
}

export interface Login {
    phone: string;
    password: string;
}

export interface Register {
    phone: string;
    password: string;
}